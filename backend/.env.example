# SIH 26183 — Cryptocurrency Investigation API
# Copy to backend/.env and fill in real values. NEVER commit backend/.env.

SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_KEY=your-supabase-service-role-key

# Required when connecting a real blockchain/indexing provider.
BLOCKCHAIN_API_URL=
BLOCKCHAIN_API_KEY=
SECRET_KEY=change-me-for-local-development

# Frontend calls the Flask API at this base URL in local development.
API_BASE_URL=http://localhost:5000/api
